import React from 'react';
import ReactDOM from 'react-dom';
import AppRouter from './AppRouter';  // Correctly reference the new file

ReactDOM.render(<AppRouter />, document.getElementById('root'));

